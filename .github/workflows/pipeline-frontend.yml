name: Deploy with Docker Compose

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Create directory and copy docker-compose file
        run: |
          cp docker-compose.yml /opt/myapp/

      - name: Run Docker Compose
        run: |
          cd /opt/myapp/
          docker-compose down
          docker-compose up -d